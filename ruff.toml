line-length = 110

[lint]
# pyflakes (F) catches real bugs — undefined names, unused imports.
# E4/E7/E9 catch import, statement, and syntax errors. Style-only rules are off.
select = ["E4", "E7", "E9", "F", "I"]
