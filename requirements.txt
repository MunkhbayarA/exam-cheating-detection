# Shared dependency pin. Also used verbatim by Streamlit Community Cloud,
# which is why the CPU torch index is listed: on a fresh install pip resolves
# torch 2.5.1+cpu (local-version tags sort above 2.5.1, so the CPU wheel wins).
#
# For CUDA training, install the matching torch build FIRST, then the rest:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# (pip leaves the already-installed +cu121 build in place.)

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1
numpy>=2.0
pandas>=2.0
opencv-python>=4.9
scikit-learn>=1.4
matplotlib>=3.8
gradio>=4.0
streamlit>=1.35
huggingface_hub>=0.23

# dev / test
pytest>=8.0
